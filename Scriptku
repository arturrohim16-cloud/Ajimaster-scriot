#!/bin/bash
# Script by Ajimaster

dashboard_utama() {
    clear
    echo -e "   \e[1;35m╔═══════════════════════════════════════════════════╗\e[0m"
    echo -e "   \e[1;35m║\e[0m         \e[1;33m📊 DASHBOARD AJIMASTER SCRIPT 📊\e[0m          \e[1;35m║\e[0m"
    echo -e "   \e[1;35m╚═══════════════════════════════════════════════════╝\e[0m"
    echo -e "    [01] SSH & OpenVPN      [11] Edit Script"
    echo -e "    [02] Vmess Manager      [12] Shadow Manager"
    echo -e "    [03] Vless Manager      [13] Speedtest"
    echo -e "    [04] Trojan Manager     [14] Create SSL"
    echo -e "    [05] Bot Panel          [15] Install UDP"
    echo -e "    [06] Delete User        [16] Restart Service"
    echo -e "    [07] Renew User         [17] Cek User"
    echo -e "    [08] Backup & Restore   [18] HARGA KUOTA (XL/AXIS)"
    echo -e "    [09] Update Script      [19] REPAIR SYSTEM"
    echo -e "    [10] Reboot VPS         [00] Exit"
    echo ""
    read -p "  Pilih Nomor: " opt
    case $opt in
        1) add_ssh ;;
        2) add_vmess ;;
        3) add_vless ;;
        4) add_trojan ;;
        5) install_bot_panel ;;
        6) del_user ;;
        7) renew_user ;;
        8) backup_restore ;;
        9) update_script ;;
        10) reboot ;;
        11) nano $0 ;;
        12) shadow_manager ;;
        13) run_speedtest ;;
        14) create_ssl ;;
        15) install_udp ;;
        16) restart_services ;;
        17) cek_user ;;
        18)
            clear
            echo "📊 LIST HARGA KUOTA"
            echo "Link WA: https://wa.me/qr/GXNINYUQLUVBN1"
            read -p "Tekan Enter Untuk Kembali..."
            dashboard_utama ;;
        19)
            clear
            echo "🛠️ MENU REPAIR"
            apt update --fix-missing
            echo "Selesai!"
            sleep 2
            dashboard_utama ;;
        00) exit ;;
        *) dashboard_utama ;;
    esac
}

# --- JEMBATAN PERINTAH ---
menu() {
    dashboard_utama
}

# --- JALANKAN MENU ---
dashboard_utama
